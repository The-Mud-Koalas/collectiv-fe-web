import React from 'react';

interface Props {
  eventId: string;
}

// queryKey: ["current-event"]

const ParticipantQRModal: React.FC<Props> = ({ eventId }) => {
  return (
    <div>ParticipantQRModel</div>
  )
}

export default ParticipantQRModal